# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
    tags:
      - '*'  # Trigger per tutti i tag 
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write
  id-token: write
  actions: write

jobs:
  build:

    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0  # Clona tutta la history Git 
    - name: Set timezone to Europe/Rome
      run: sudo timedatectl set-timezone Europe/Rome
    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Check NVD_API_KEY
      run: |
        if [ -z "$NVD_API_KEY" ]; then
          echo "NVD_API_KEY is NOT set!"
        else
          echo "NVD_API_KEY is set. Length: ${#NVD_API_KEY}"
        fi
      env:
        NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    - name: Check OSS_INDEX_USER
      run: |
        if [ -z "$OSS_INDEX_USER" ]; then
          echo "OSS_INDEX_USER is NOT set!"
        else
          echo "OSS_INDEX_USER is set."
        fi
      env:
        OSS_INDEX_USER: ${{ vars.OSS_INDEX_USER }}
    - name: Check OSS_INDEX_PASSWORD
      run: |
        if [ -z "$OSS_INDEX_PASSWORD" ]; then
          echo "OSS_INDEX_PASSWORD is NOT set!"
        else
          echo "OSS_INDEX_PASSWORD is set."
        fi
      env:
        OSS_INDEX_PASSWORD: ${{ secrets.OSS_INDEX_PASSWORD }}

    - name: Get date for cache key
      id: date
      run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

    - name: Read OWASP plugin version from pom.xml
      id: owasp
      run: |
        VERSION="$(mvn -q -DforceStdout help:evaluate -Dexpression=owasp.plugin.version)"
        echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        echo "OWASP Dependency-Check version: $VERSION"

    - name: Read project version from pom.xml
      id: version
      run: |
        VERSION="$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)"
        echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        echo "Project version: $VERSION"

    - name: Cache OWASP Dependency-Check DB
      id: owasp-cache
      uses: actions/cache@v5
      with:
        path: .dependency-check/data
        # Chiave: versione plugin (bump -> cache invalidata per schema diverso)
        # + data (allineamento con il refresh giornaliero del DB NVD).
        key: ${{ runner.os }}-owasp-v${{ steps.owasp.outputs.version }}-${{ steps.date.outputs.date }}
        restore-keys: |
          ${{ runner.os }}-owasp-v${{ steps.owasp.outputs.version }}-

    - name: Build War with Maven
      run: mvn clean install -P war -DskipTests -Ddependency-check.skip=true
    - name: Build Jar with Maven
      env:
        NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        OSS_INDEX_USER: ${{ vars.OSS_INDEX_USER }}
        OSS_INDEX_PASSWORD: ${{ secrets.OSS_INDEX_PASSWORD }}
        # Se la cache è esatta (stessa giornata) skippa l'update NVD
        NOUPDATE_FLAG: ${{ steps.owasp-cache.outputs.cache-hit == 'true' && '-DautoUpdate=false' || '' }}
      run: mvn install -P jar -Dspring.profiles.active=test -Duser.timezone=Europe/Rome -DnvdApiKey=$NVD_API_KEY -DossIndexUsername=$OSS_INDEX_USER -DossIndexPassword=$OSS_INDEX_PASSWORD -DdataDirectory=.dependency-check/data -Dowasp.format=ALL -Dowasp.output.dir=./dependency-check-report $NOUPDATE_FLAG

    - name: Show JaCoCo report location and Zip JaCoCo HTML report
      run: |
        ls -lh target/site/jacoco/
        file target/site/jacoco/jacoco.xml || echo "Report non trovato"
        cd target/site
        zip -r ../jacoco-html-report.zip jacoco
        cd ../../
        ls -lh target/  
    
    - name: Copy dependencies
      run: |
        mkdir -p target/dependency
        mvn dependency:copy-dependencies -DoutputDirectory=target/dependency

    - name: SonarCloud Scan
      uses: SonarSource/sonarqube-scan-action@v5
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        SONAR_HOST_URL: https://sonarcloud.io
      with:
        args: >
          -Dsonar.projectKey=link-it_govpay-gde-api
          -Dsonar.organization=link-it
          -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
          -Dsonar.sources=src/main/java
          -Dsonar.tests=src/test/java
          -Dsonar.java.binaries=target/classes
          -Dsonar.coverage.exclusions=**/test/**,**/*Test*.java
          -Dsonar.java.source=21
          -Dsonar.java.libraries=target/dependency

    - name: License Analysis
      run: |
        set -euo pipefail
        echo "Downloading license information..."
        mvn org.codehaus.mojo:license-maven-plugin:2.4.0:aggregate-download-licenses -DexcludeScopes=test,provided,system -DincludeTransitiveDependencies=true
        echo "Analyzing licenses..."
        python3 .github/workflows/scripts/analyze_licenses.py --exceptions .github/workflows/scripts/license-exceptions.json

    - name: Archive licenses analysis results
      uses: actions/upload-artifact@v7
      with:
        name: third-party-licenses
        path: third-party-licenses

    - name: Upload artifact to GitHub
      uses: actions/upload-artifact@v7
      with:
        name: govpay-gde-api
        path: |
          target/govpay-gde-api.*
          target/site/jacoco/jacoco.xml
          target/jacoco-html-report.zip
          target/dependency-check-report.html
          target/dependency-check-report.xml

  # ── OSV Scan (reusable workflow) ──────────────────────────────────────
  osv-scan:
    if: ${{ github.event_name == 'push' && (github.ref_name == 'main' || github.ref_type == 'tag' || vars.FORCE_OSV_JOB == 'true') }}
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@v2.3.5"
    with:
      matrix-property: ''
      fail-on-vuln: ${{ github.ref_name == 'main' || github.ref_type == 'tag' }}
      results-file-name: osv-report.sarif
      scan-args: |-
        -r
        .
        --lockfile
        ./pom.xml

  # ── SBOM CycloneDX ───────────────────────────────────────────────────
  sbom:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: >-
      vars.DISABLE_SBOM_JOB != 'true' &&
      (github.event_name == 'push' &&
       (github.ref_name == 'main' || github.ref_type == 'tag' || vars.FORCE_SBOM_JOB == 'true'))
    continue-on-error: ${{ vars.FORCE_SBOM_JOB == 'true' && github.ref_type != 'tag' && github.ref_name != 'main' }}
    env:
      SBOM_CYCLONEDX_PLUGIN_VERSION: '2.9.1'   # ← adegua al valore reale
      SBOM_CYCLONEDX_SCHEMA_VERSION: '1.6'     # ← adegua al valore reale

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Generate SBOM (CycloneDX json + xml)
        run: |
          mkdir -p sbom/cyclonedx
          echo "Generazione SBOM CycloneDX (json + xml) ..."
          mvn org.cyclonedx:cyclonedx-maven-plugin:${SBOM_CYCLONEDX_PLUGIN_VERSION}:makeAggregateBom \
            -DoutputDirectory=sbom/cyclonedx \
            -DoutputName=bom.cdx \
            -DoutputFormat=all \
            -DschemaVersion=${SBOM_CYCLONEDX_SCHEMA_VERSION} \
            -DskipNotDeployed=false \
            -DincludeTestScope=false
          ls -la sbom/cyclonedx/

      - name: Upload SBOM artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: sbom-report
          path: sbom/
          retention-days: 30

  # ── GitHub Release (solo tag) ────────────────────────────────────────
  # ── Immagine Docker di sviluppo (solo push su main, solo SNAPSHOT) ────
  # Serve a eseguire i test di integrazione prima di un rilascio: pubblica su
  # un repo separato (linkitaly/govpay-gde-api-dev) cosi' una SNAPSHOT non finisce mai
  # nel repo delle immagini di produzione.
  # Usa Dockerfile.daFile, che prende il jar dal build context: Dockerfile.github
  # scarica il jar da una GitHub release, che per una SNAPSHOT non esiste.
  docker-dev:
    runs-on: ubuntu-latest
    needs: build
    if: ${{ github.event_name == 'push' && github.ref_name == 'main' }}

    steps:
    - name: Skip (not a SNAPSHOT)
      if: ${{ !endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      run: echo "Versione ${{ needs.build.outputs.version }} non e' una SNAPSHOT -> nessuna immagine di sviluppo pubblicata."

    - name: Checkout code
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      uses: actions/checkout@v6

    - name: Download jar from build job
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      uses: actions/download-artifact@v7
      with:
        name: govpay-gde-api
        path: artifacts/

    - name: Set up Docker Buildx
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      uses: docker/setup-buildx-action@v4

    - name: Login to Docker Hub
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      uses: docker/login-action@v4
      with:
        username: ${{ vars.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Prepare build context
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      run: |
        set -euo pipefail
        cd docker
        rm -rf buildcontext
        mkdir -p buildcontext/
        cp -fr commons buildcontext/
        cp -f ../artifacts/govpay-gde-api.jar buildcontext/
        ls -lh buildcontext/

    - name: Build and push development image
      if: ${{ endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
      uses: docker/build-push-action@v6
      with:
        context: docker/buildcontext
        file: docker/govpay-gde/Dockerfile.daFile
        push: true
        build-args: |
          govpay_gde_fullversion=${{ needs.build.outputs.version }}
        tags: |
          linkitaly/govpay-gde-api-dev:${{ needs.build.outputs.version }}

  release:
    runs-on: ubuntu-latest
    needs: [ build, osv-scan, sbom ]
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    # Con il versionamento SNAPSHOT su main, un tag applicato senza aver prima
    # tolto -SNAPSHOT dal pom produrrebbe una release costruita da uno SNAPSHOT.
    # Il tag deve inoltre coincidere con la versione del pom, perche' gli asset
    # della release e il download in Dockerfile.github sono nominati sul tag.
    - name: Verify tag matches a non-SNAPSHOT pom version
      run: |
        set -euo pipefail
        VERSION="$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)"
        TAG="${{ github.ref_name }}"
        echo "pom version: $VERSION"
        echo "tag        : $TAG"
        case "$VERSION" in
          *-SNAPSHOT)
            echo "::error::Il pom e' alla versione $VERSION. Togliere -SNAPSHOT e ricreare il tag prima di rilasciare."
            exit 1
            ;;
        esac
        if [ "$VERSION" != "$TAG" ]; then
          echo "::error::Il tag ($TAG) non coincide con la versione del pom ($VERSION). Gli asset della release e il download in Dockerfile.github sono nominati sul tag."
          exit 1
        fi
        echo "Versione e tag coincidono, procedo."

    # Artifact build (JAR, WAR, report OWASP e JaCoCo)
    - name: Download govpay-gde-api artifact
      uses: actions/download-artifact@v7
      with:
        name: govpay-gde-api
        path: jar/

    # Report — finiranno nello ZIP
    - name: Download third-party-licenses
      uses: actions/download-artifact@v7
      with:
        name: third-party-licenses
        path: reports/third-party-licenses/

    - name: Download OSV Scanner SARIF
      uses: actions/download-artifact@v7
      with:
        name: OSV Scanner SARIF file
        path: reports/osv/

    - name: Download SBOM
      uses: actions/download-artifact@v7
      with:
        name: sbom-report
        path: reports/sbom/

    - name: Rename artifacts with tag version
      run: |
        cp jar/govpay-gde-api.war jar/govpay-gde-api-${{ github.ref_name }}.war
        cp jar/govpay-gde-api.jar jar/govpay-gde-api-${{ github.ref_name }}.jar

    - name: Assemble reports ZIP
      run: |
        mkdir -p reports/owasp reports/jacoco reports/osv reports/sbom

        # OWASP e JaCoCo dall'artifact build
        cp jar/dependency-check-report.html reports/owasp/ 2>/dev/null || true
        cp jar/dependency-check-report.xml  reports/owasp/ 2>/dev/null || true
        cp jar/site/jacoco/jacoco.xml       reports/jacoco/ 2>/dev/null || true
        cp jar/jacoco-html-report.zip       reports/jacoco/ 2>/dev/null || true

        # pipeline info
        echo "Pipeline Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
          > reports/${{ github.ref_name }}_actions_run.txt

        zip -r release-reports-${{ github.ref_name }}.zip reports/
        ls -lh release-reports-${{ github.ref_name }}.zip

    - name: Create GitHub release
      uses: softprops/action-gh-release@v2.2.1
      with:
        files: |
          jar/govpay-gde-api-${{ github.ref_name }}.war
          jar/govpay-gde-api-${{ github.ref_name }}.jar
          release-reports-${{ github.ref_name }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

  # ── Docker build & push (solo tag, dopo release) ─────────────────────
  docker:
    runs-on: ubuntu-latest
    needs: release
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ vars.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Extract version from tag
      id: version
      run: echo "VERSION=${{ github.ref_name }}" >> $GITHUB_OUTPUT

    - name: Prepare build context
      run: |
        cd docker
        rm -rf buildcontext
        mkdir -p buildcontext/
        cp -fr commons buildcontext/

    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: docker/buildcontext
        file: docker/govpay-gde/Dockerfile.github
        push: true
        build-args: |
          govpay_gde_fullversion=${{ steps.version.outputs.VERSION }}
        tags: |
          linkitaly/govpay-gde-api:${{ steps.version.outputs.VERSION }}
          linkitaly/govpay-gde-api:latest
